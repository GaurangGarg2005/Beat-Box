# Beat-Box
This project is a Spotify-like Music Player application developed using C++ with the Qt framework (Qt Quick/QML). The application follows a modular architecture where the backend logic is implemented in C++ and seamlessly connected to a modern, responsive user interface built using QML.
